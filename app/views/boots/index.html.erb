<div><p class="btn btn-success"> This store has <%=pluralize(@boots.size, 'pair') %> added!</p>
<p class="pull-right"><%= link_to 'New Boots', new_boot_path, :class => 'btn btn-primary', :remote => true, :id => 'new_boot_link' %></p></div>

<table class="table" id="boots_table">
  <thead style="text-align:center">
    <tr>
      <th style="text-align:center"><%=t '.actions', :default => t("helpers.actions") %></th>
      <th style="text-align:center">Number</th>
      <th style="text-align:center">Size</th>
      <th style="text-align:center">Condition</th>  
      <th style="text-align:center">Brand</th>
      <th style="text-align:center">Location</th>
      <th style="text-align:center">Status</th>
    </tr>
  </thead>
  <tbody>
 		<%= render :partial => @boots %>
  </tbody>
</table>
